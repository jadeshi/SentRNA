A list of models that can solve certain difficult puzzles from the Eterna100. The puzzle name, where the model is located, and the model name are listed.
For each model, in the directory given, there is a "results" and "test" directory. To use the model, copy all items containing the model name from "results" and "test" into a new "results" and "test" directory in the SentRNA directory, and then use SentRNA/run.py with --mode test and other appropriate arguments (see SentRNA/test_kyurem7.py for an example)

Kyurem 7    models/trained_models/batch2    val_test_n_samples=2_force_add_features=False_threshold=2_shuffle=True_random_append=0_train_on_solved

Kyurem 5    models/trained_models/batch2    val_test_n_samples=2_force_add_features=False_threshold=2_shuffle=True_random_append=0_train_on_solved

"1,2,3and4bulges"    models/trained_models/batch1    trial-14_MI-20

Loop next to a Multiloop    models/trained_models/batch1    trial-1_MI-26

Crop circle 2    models/trained_models/batch2    val_test_n_samples=8_force_add_features=False_threshold=2_shuffle=False_random_append=0_train_on_solved

Mat - Lot 2-2 B    models/trained_models/matlot    MI-54_trial4

hard Y    models/trained_models/batch1    trial-16_MI-22

Mutated chicken feet    models/trained_models/mcf    3loop_7
